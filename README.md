# C# 演算法筆記
## C++ 演算法實作與學習
>
## 圖 Graph
> ## 最小生成樹
>> * Prim演算法：
>> 從任意一個頂點開始，每次選擇一個與當前頂點集最近的一個頂點，並將兩頂點之間的邊加入到樹中。Prim演算法在找當前最近頂點時使用到了貪婪演算法。時間複雜度為O(n^2)。
>> * Kruskal演算法：
>> 按照邊權值從小到大排序，依次加入生成樹中，如果加入某條邊會形成環則不加入。時間複雜度為O(mlogn)，其中m為邊數，n為節點數。
